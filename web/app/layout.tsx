// app/layout.tsx (MUST be directly in app/)
import './globals.css'; // Ensures Tailwind / global styles render

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="bg-slate-950 text-slate-100 antialiased">
        {children}
      </body>
    </html>
  );
}